{
  "name": "BigOne Sports Predictions",
  "description": "Sports predictions dashboard with Flask API and Next.js frontend",
  "buildpacks": [
    { "url": "heroku/python" }
  ],
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "description": "API base URL for frontend (empty = same origin)",
      "value": ""
    }
  }
}
