# ⚠️ DEPRECATED

This frontend has been superseded by `sports-dashboard/` (Next.js).

**Do not use this directory for new development.**
All UI work targets `sports-dashboard/` which deploys to Vercel.

This directory will be removed in a future cleanup.
