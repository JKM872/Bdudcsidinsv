# ⚠️ DEPRECATED

This directory contains a **legacy API server** that has been superseded by the root-level `api_server.py`.

**Do not use `api/app.py` for new development.** All endpoints are served by `api_server.py` which runs on Heroku.

This directory will be removed in a future cleanup.
